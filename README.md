# Automatic Website

Fully automatic Next.js static site with live search, sitemap, universal header/footer.

## Setup
1. `npm install`
2. `npm run generate-all`
3. `npm run dev` for preview
4. `npm run build` for production

Add posts in `content/blog/*.md` – rebuilds auto-index.
Deploy: Push to GitHub, import to Vercel.